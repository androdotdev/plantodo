# TODO — next update

> Confirmed work items only. Speculative ideas go in discussions, not here.

## Prioritized

- [ ] **Decide brand name** — Relay / Pagelish / other. Once decided, rename scope includes:

  - `web/` — dashboard copy, page title, meta tags, favicon
  - `cli/` — `name` in `package.json` and CLI `--version` string
  - `AGENTS.md`, `README.md`, `cli/README.md`, `SECURITY.md`
  - GitHub repo description and topics
  - Domain (if changing)

- [ ] **Terminology migration** (can do independently of brand name):

  | File | Changes |
  |---|---|
  | `web/app/api/mcp/lib.ts` | `upload_post → publish_page`, `replace_post → update_page`, descriptions |
  | `cli/src/index.ts` | command names, help text |
  | `web/public/SKILL.md` | agent-facing language |
  | `web/public/llms.txt` | LLM context |
  | Dashboard pages | "Post" → "Page", "Upload" → "Publish" |
  | `web/app/p/[id]/route.ts` | internal comments |

- [ ] **DB driver: evaluate WebSocket mode** — `web/db/index.ts` uses `neon-http` (per-query HTTP round-trip). Switch to `@neondatabase/serverless` WebSocket mode for ~150ms warm latency reduction on multi-query operations like `replace_post`. Verify no Vercel Serverless regression.

- [ ] **MCP performance: measure and log** — add request duration logging to `route.ts` / `lib.ts` so we can quantify actual P50/P95 before optimizing further.
- [ ] **API key prefix bug** — CLI stores key as `postbyezbmr…` (bare `post`), but server generates keys with `post_` prefix. The underscore gets lost somewhere in the setup flow. Also check MCP token prefix (`mcp_`).
- [ ] **Markdown upload support** — `--mark` flag on CLI `upload`/`replace`; server converts markdown → HTML:

  - `web/db/schema.ts` — add `type` column to `posts` (TEXT, default `"html"`; values `html` | `markdown`). String field, not a boolean — more content types later
  - `web/app/api/posts/route.ts` + `web/app/api/posts/[id]/route.ts` — accept `type` in body; when `markdown`, convert with a markdown→HTML lib before storing (upload and `replace` do the same)
  - `cli/src/index.ts` — `upload`/`replace` check `--mark` and send `type: "markdown"`; skip DOCTYPE validation for markdown; derive title from first `#` heading
  - MCP `upload_post`/`replace_post` — optional `type` arg for parity
  - New migration for the column
- [ ] **CLI UX: `post setup` double-pastes the key** — pasted key got written twice (`postbyezbmr…postbyezbmr…AAgL`, 133 chars) → 401. The prompt must capture exactly one read. Also: silent prompt hides keystrokes but piped input path needs the same guard.
- [ ] **CLI: `--title` flag on `upload`/`replace`** — title currently comes only from the HTML `<title>` tag (opaque, undocumented in `--help`); old posts show filename-ish titles in `list`. Flag should win over extracted title.
- [ ] **CLI: terse errors** — `✗ Error 401: Unauthorized` gives no recovery hint. Include "get a key at <dashboard>" on 401, and context per command (which post/URL failed).
- [ ] **Origin isolation friction: proxy route table** — replace string-prefix matching (`path.startsWith("/p/")`) with an explicit route table so sub-paths (`/p/:id/edit`, data admin) are predictable; fixes trailing-slash token bug, stale-token double-`key` param, and edit-link breakage on the posts domain in one pass.
- [ ] **Unify dual rendering paths** — `{{placeholder}}` server interpolation vs `window.__PH_DATA` client injection are two overlapping systems; posts break by picking one accidentally (resume post did). Define one contract: static fields interpolate server-side, dynamic/JS access via `__PH_DATA`; document it in SKILL.md.
- [ ] **Remove dead code** — dashboard `UploadPost.tsx` (no importers), `posts-store.ts` `listLoaded` (never read), bulk `DELETE /api/posts`. Keep `verification` table (Better Auth owns its schema).
- [ ] **CLI: plaintext key storage** — `~/.post/config.json` stores the API key in plaintext. At minimum chmod 600 on write; consider keyring integration.

> **Open question — `data` public-by-default:** GET `/api/posts/:id/data` is public unless the post is private, so a post's JSON blob is readable by anyone with the URL. Needed for share-links, but no middle ground exists for "public page, private data". Options: private-post-only data, per-key visibility, or document + encrypt client-side. **Discuss before deciding.**

- [ ] **Dashboard: rename "API Keys" section to "Get Started"** — merge Agent Setup Prompt (better UI) + API key generation into one section. Sidebar label + section content.

## Pre-major-update fixes (research audit — 2026-07-31)

> Findings from a 4-agent read-only audit (bugs / security / refactor / launch gaps). Lines verified by reading source; fix directions included, no code changed.

### Must fix — confirmed bugs
- [ ] **Dashboard "Save Data" nests post data under a `data` key** — `web/app/post/edit/[id]/page.tsx:95` sends `{data: parsed}`, but PATCH `/api/posts/:id/data` merges the body directly (CLI + MCP send the raw object). `{{placeholder}}` rendering breaks for dashboard saves. Fix: send `JSON.stringify(parsed)`.
- [ ] **Private posts served with `Cache-Control: public, max-age=300`** — `web/app/p/[id]/route.ts:162-169`; denial paths use `private, no-store` (lines 103-115). Private HTML + `window.__PH_DATA` + `?key=` token cacheable by shared caches. Fix: `private, no-store` when `post.isPrivate`.
- [ ] **Public GET /api/posts/:id leaks owner `userId`** — `db.select()` returns the full row (`web/app/api/posts/[id]/route.ts:16-24`); same for `/data`. Fix: project only intended fields.
- [ ] **PATCH /api/posts/:id accepts mixed-type garbage** — AND-chain guard (`[id]/route.ts:59`); `{html: 123, isPrivate: true}` persists non-strings. Fix: per-field type validation.
- [ ] **CLI `setup` ignores `POST_API_KEY`** — documented non-interactive flow (`cli/README`) prompts instead of using the env var. Related to the known `post_` prefix bug.
- [ ] **`/p/:id/` trailing slash breaks private access** — `web/proxy.ts:48` signs the token for `id/` (`path.replace("/p/","")`), route compares against normalized `id` → owner gets 403. Fix: strip trailing slash before signing.
- [ ] **Proxy appends a second `key` param when a stale token is in the URL** — `web/proxy.ts:52-55`; viewer reads the first (stale) token → 403 for the owner. Fix: replace existing `key`.
- [ ] **`withError` returns 500 with raw `err.message`** — malformed JSON body → 500 + parser text; DB errors echoed (`web/lib/with-error.ts:7-15`). Fix: 400 on JSON parse errors, generic 500, log server-side.

### Must fix — credential lifecycle / security
- [ ] **MCP "Generate New URL" leaves the previous token active** — no server-side uniqueness (`web/app/api/keys/route.ts:27-48`); dashboard `generateMcpKey` doesn't revoke (`McpSection.tsx:37-66`). AGENTS.md "replace in place" claim false. Fix: revoke existing `mcp_` key on generate.
- [ ] **Private-post capability tokens can't be revoked** — 30-day HMAC, no version (`web/lib/post-token.ts:4-28`); private→public→private keeps old tokens valid. Fix: token-version column on `posts`, rotate on visibility toggle.
- [ ] **Dashboard-created API keys: no rate limit, no expiry by default** — `POST /api/keys` sets `rateLimitEnabled: false` unless checked; `expiresIn` never sent; plugin default null (`web/lib/auth.ts:22-29`). Decide policy before launch.

### Verify / decide before major update
- [ ] **Drizzle migration journal missing** — `web/drizzle/` has 6 migration folders, no `meta/_journal.json`; documented `bun -C web db:migrate` flow likely broken ([INFERENCE] — regenerate journal + verify on scratch DB).
- [ ] **`@better-auth/drizzle-adapter` peer mismatch** — requires `drizzle-orm ^0.45.2`, repo pins `1.0.0-rc.4` (bun.lock:109). Verify compatibility (ideally on Drizzle 1.0 stable).
- [ ] **API-key quota double-consumed** — proxy re-verifies auth per `/api/posts*` request; every route re-verifies (`web/proxy.ts:67`, routes). Remaining/rate counters may decrement twice per request ([INFERENCE], plugin internals unverified). Decide: drop proxy auth block (keep header strip) or make GETs consume `x-user-id`.
- [ ] **No rate limiting on public endpoints** — public GETs + failed auth are unthrottled; each costs a Neon round trip (SECURITY.md declares out of scope — revisit before launch).
- [ ] **CI gaps** — `.github/workflows/ci.yml` `build-web` has `continue-on-error: true`; no typecheck anywhere; `vercel.json` `ignoreCommand` skips main. Wire typecheck + fail on build error.

### Refactor / polish
- [ ] Dead code: `web/app/dashboard/components/UploadPost.tsx` (no importers), `posts-store.ts` `listLoaded` (never read), bulk `DELETE /api/posts` (unused).
- [ ] API shape drift REST vs MCP: `list_posts` omits `isPrivate`; `upload_post` has no private flag; keys routes use lowercase errors + a second auth idiom (`web/app/api/keys/route.ts`).
- [ ] De-dup `BASE_URL` (×6, diverging defaults) and `MAX_HTML_SIZE` (×3) into shared modules.
- [ ] CLI polish: `replace` prints "[public]" for an unchanged private post; `--private --public` conflict resolves silently; `api()` crashes on non-JSON error bodies.
- [ ] Dashboard/edit UX: fetch failures silently swallowed (`dashboard/page.tsx:115-165`); edit page can spin forever on load error (`post/edit/[id]/page.tsx:52-63`).
- [ ] `/api/mcp` is POST-only but documented as GET/POST (`AGENTS.md`) — add GET or fix docs.
- [ ] Docs refresh: AGENTS.md (migration count "2" → 6, x-user-id auth-flow claim false, env list missing `POST_TOKEN_SECRET`, design-token table), cli/README env-priority contradiction, `.env.example` stale `ptd` section.

## Done (this session)

- [x] CLI README — full docs for every command
- [x] Silent stdin prompt (`readSilent` in `cli/src/index.ts`)
- [x] Proxy test mock fix (`new NextResponse` support)
- [x] MCP tool descriptions updated to explain `{{placeholder}}` → data relationship
- [x] SKILL.md / llms.txt restructured with "templates + data" as core concept
- [x] CLI version bumped to `0.9.0`, tag `cli-v0.9.0` created locally
- [x] Security review — no action needed
